o add a bare command field to job record (basename($0))
o change job record start_time to start_ts
o add a start_time time-only field to job record
o add stdout and stderr paths to job record (if any)
o have both bare and fully-qualified hostnames in job record
o add stdout and stderr file sizes to job record
o make sure timestamps are recording in GMT, not localtime
o add a die handler to cronologue
o add a meta refresh header to gui
- get rss feed working


- add a config item and option to indicate "don't record job record unless there's output"
- add a -c|--config option to specify config file
- add a -S|--server option to specify server
- add a use_fully_qualified_hostnames config item
- add previous and next links to date-based pages


